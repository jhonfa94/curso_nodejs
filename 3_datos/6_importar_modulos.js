/* LLAMAMOS EL MODULO miModulo.js QUE SE CREO MANUALMENTE */

//llamamos el modulo con el mismo nombre, no se debe especicar la extension del archivo
var miModulo = require('./miModulo');

miModulo.multiplicar(10);
miModulo.dividir(10);

